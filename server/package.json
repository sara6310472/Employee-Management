{
  "name": "employee-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "dev": "nodemon index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}